seed: 1337
data:
  root: /kaggle/working/data
  archive_root: /kaggle/input/xnerf-malware-archives/archives
  train_manifest: /kaggle/working/data/processed/manifest.jsonl
  val_manifest:
model:
  num_classes: 2
  num_families: 32
training:
  batch_size: 4
  lr: 0.0003
  epochs: 5
  grad_accum: 4
  num_workers: 2
  checkpoint_dir: /kaggle/working/checkpoints
  patience: 3
export:
  checkpoint: /kaggle/working/checkpoints/best.pt
  output: /kaggle/working/export/xnerf_local_inference.pt
