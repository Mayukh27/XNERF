seed: 1337
data:
  root: data
  archive_root: data/archives
  full_manifest: data/processed/manifest.jsonl
  train_manifest: data/processed/train_manifest.jsonl
  val_manifest: data/processed/val_manifest.jsonl
  test_manifest: data/processed/test_manifest.jsonl
model:
  num_classes: 2
  num_families: 32
training:
  batch_size: 1
  lr: 0.0003
  epochs: 1
  grad_accum: 1
  num_workers: 4
  checkpoint_dir: checkpoints
  patience: 1
local_inference:
  checkpoint: models/xnerf_local_inference.pt
  note: "Train on Kaggle; use this PC for API/inference only."
